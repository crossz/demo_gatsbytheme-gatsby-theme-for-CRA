{
  "name": "gatsby-starter-theme-workspace",
  "private": true,
  "version": "0.0.1",
  "license": "0BSD",
  "scripts": {
    "develop": "yarn workspace gatsby-starter-default develop",
    "build": "yarn workspace gatsby-starter-default build",
    "serve": "yarn workspace gatsby-starter-default serve",
    "clean": "yarn workspace gatsby-starter-default clean"
  },
  "workspaces": [
    "gatsby-starter-default",
    "gatsby-theme-minimal",
    "example",
    "my-cra-app"
  ]
}
